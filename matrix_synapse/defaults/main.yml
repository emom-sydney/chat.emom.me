#SPDX-License-Identifier: MIT-0
---
# defaults file for matrix_synapse

matrix_server_name: "{{ matrix_domain }}"
matrix_listen_port: 8008
matrix_report_stats: "no"

matrix_registration_shared_secret: "obloid_dissonance_vertigo"

matrix_data_dir: "/var/lib/matrix-synapse"
matrix_homeserver_config_directory: "/etc/matrix-synapse/conf.d"
